# DataAgent 项目汇报

> **汇报对象**:业务负责人
> **汇报时间**:2026 年 8 月
> **项目版本**:v1.0(Demo)
> **项目负责人**:Ronnie
> **项目代码位置**:`/Users/ronnieliu/Documents/code/DataAgent/`

---

## 1. 项目背景与目标

### 1.1 业务痛点

医药 SFE(Sales Force Effectiveness)团队的日常分析高度依赖 Excel + Python 脚本 + 数据组同事,典型工作流存在以下问题:

| 现状问题 | 对业务的影响 |
|---|---|
| 代表想看「川渝大区各代表增长率 top10」这类问题要先提工单给数据组 | 一个简单问题 1-2 天才能拿到答案 |
| 数据组 80% 精力在做重复的「写 SQL / 取数 / 出表」 | 真正有价值的分析(洞察、归因、建议)反而没时间做 |
| 业务口径靠口口相传或散落在 Excel 批注里 | 同名指标不同人算出来结果不一样(度量值混淆、新品铺货异常) |
| 合规要求高,但分析过程缺乏审计痕迹 | 《医药代表管理办法》(2026.8.1 施行)压力下,谁问的、按什么口径算的,留痕不完整 |
| 业务人员不会 Python,也不会写 SQL | 想自己分析但门槛高,只能排队 |

### 1.2 项目目标

**做一个面向 SFE 业务人员的 AI 数据分析 Agent**,让销售总监、地区经理等业务角色能用**自然语言(中文)**直接跟数据对话,30 秒拿到分析结果,而不是排队等 2 天。

### 1.3 项目定位

- **不是**:通用 BI 工具(不做看板、不做 ETL、不替代数据仓库)
- **不是**:AutoML / 数据科学平台(不训练模型、不做预测)
- **是**:业务人员的"问数助手"——把数据组的取数工作自动化,把分析师的洞察工作提速 5 倍

---

## 2. 已交付功能(v1.0)

### 2.1 核心对话能力

业务人员在前端用中文提问,Agent 自动:
1. **理解问题**(LLM 解析意图,结合业务规则)
2. **生成代码**(LLM 输出可执行的 pandas 代码)
3. **沙箱执行**(白名单机制,只允许安全的 pandas/numpy 操作,禁止文件/网络)
4. **错误自愈**(代码报错自动重试 2 次,把错误回传给 LLM 重写)
5. **返回数据表格**(JSON 化,前端渲染)

**实测样例**:
- "学术接受度增长率 top10 的代表" → 10 行表(代表、当前值、同期值、增长率)
- "按大区看哪个增长率最高" → 7 大区聚合
- "G-川渝大区各代表学术接受度总和" → 区域级数据
- "统计每个产品的代表覆盖数" → 产品维度表

### 2.2 多轮对话(同一 Session 内)

- 单 session 支持最多 5 轮上下文,业务人员可以基于上一轮结果继续问:
  - "增长率 top10" → "只看川渝大区" → "其中山东的再加一列同期值"
- LLM 能理解"再加个条件""换成大区"等省略主语的追问

### 2.3 AI 洞察 + 后续问题建议(可选)

每个结果可点击"AI 洞察"按钮:
- LLM 基于结果数据生成 1-2 句**带具体数字**的业务洞察(例:"Top10 中 G-浙皖大区占 3 席,增长率均超 50x")
- 推荐 3 个用户可能想继续问的具体问题(不是泛泛而谈)

### 2.4 一键出图(可视化)

点击"生成图表"按钮:
- LLM 根据数据结构选图表类型(柱状图 / 折线图 / 饼图 / 散点图 / 横向柱状图)
- 支持 5 种图表自动配置,前端 ECharts 渲染
- 业务人员不用懂"该用啥图",AI 自动选

### 2.5 一键出报告(单问题)

点击"生成分析报告"按钮:
- LLM 生成结构化中文报告,5 个章节:分析概述 → 数据概览 → 关键发现 → 详细分析 → 结论与建议
- 引用具体数字,**不**说"作为一个 AI"那种官话,直接像内部 SFE 报告给老板看

### 2.6 会话总结报告(整轮对话)

整个对话跑完(>=1 轮),点"会话总结"按钮:
- LLM 基于整段对话的代码+结果+洞察,生成跨问题综合分析
- 适合"开完会要发会议纪要"场景

### 2.7 后台配置(零代码调整业务规则)

**核心设计**:业务规则不写死在代码里,而是放在 `web/rules.json`,业务管理员通过 Web UI 改。

支持 6 类配置:

| Section | 用途 | 业务场景 |
|---|---|---|
| 过滤规则 | LLM 必须遵守的硬规则(优先级:高(必做) / 中(默认) / 低(建议)) | "度量值必须先过滤学术接受度""离职转岗不排除" |
| 字段偏好 | 核心指标 / 次要指标 / 噪音字段 | 让 LLM 知道哪些列重要,哪些是 ID/地名 |
| 行业背景 | 行业 / 业务目标 / 合规约束 | 注入医药 SFE 场景,《医药代表管理办法》合规提示 |
| 输出格式(全局) | 增长率格式 / 小数位 / 千分位 / 中文列名 | 全局默认显示规则 |
| 列特定格式规则 | 针对特定列的覆盖(可加多条) | 金额列小数 2 位+千分位、占比列×100+% 后缀 |
| 指标计算规则 | 同比/达成/各种衍生指标的公式 | LLM 算同比用 `(cur-prev)/prev`,处理除零 |

**UI 设计**:全程**结构化表单 + 表格**,**绝不让业务人员看到 JSON**。增删都有按钮,所见即所得。

### 2.8 用户习惯记忆(自动学习)

Agent 能从历史对话中**自动学习用户偏好**,记住的偏好会影响后续回答:

**已学到 2 条**(示例):
- 用户做 topN 分析时习惯按代表维度聚合(置信度 0.85,5 次证据)
- 用户大部分分析聚焦在川渝大区(置信度 0.60,2 次证据)

**实测**:问"增长率 top10 看看"(没说区域),LLM 自动加了 `df['NEW大区'] == '川渝大区'` 过滤——因为 memory 里有这个偏好。

业务人员也可以手动管理(增/删/改/调置信度),存到 `web/user_memory.json`,所有数据落到磁盘,可审计可追溯。

### 2.9 三层优先级防护(核心架构原则)

```
1. 管理员业务规则(rules.json)  ← 优先级最高
         ↓
2. 用户习惯记忆(user_memory.json) ← 自动学习
         ↓
3. LLM 直觉                     ← 兜底
```

**核心原则**:**管理员配置优先级永远大于用户习惯**,冲突时以管理员为准。

**实现**:
- 后端做字段冲突检测:memory 里跟 rules 字段重名的 habit 自动屏蔽,不让 LLM 误读
- Prompt 显式声明:"本节优先级永远低于业务规则段,冲突时本节忽略"
- Prompt 顺序:业务规则在前,memory 在后(LLM 对前面内容天然更重视)

**应用例子**:管理员把 `compliance_constraints` 设为《医药代表管理办法》要求,即使 memory 里自动学到"不用管合规"也会被屏蔽,合规底线不会被突破。

---

## 3. 技术架构亮点

### 3.1 数据安全:沙箱代码执行

LLM 生成的 pandas 代码在受限沙箱里跑,而不是裸 `exec()`:

- **白名单机制**:只允许 `pd`(pandas)、`np`(numpy)、`re`、`json` 等安全模块
- **禁止导入**:`os`、`sys`、`subprocess`、`requests`、任何文件/网络操作
- **禁止 matplotlib**:防止 LLM 自动出图时画饼图失败污染结果
- **异常处理**:每个 sandbox 调用最多 2 次重试,把 `NameError` / `ValueError` 等错误回传给 LLM 让它重写
- **NaN/Inf 防御**:pandas 结果里的 inf 强制转 NaN,避免前端 JS 解析崩溃

**结果**:可以放心让 LLM 写代码跑数据,不用担心它删库或访问外网。

### 3.2 数据源:启动时指定,不在前端上传

**核心设计原则**:`数据源不要做成上传,而是做成指定的` —— 业务合规要求。

| 配置方式 | 适用场景 | 命令行示例 |
|---|---|---|
| Excel 文件 | Demo / 小数据 | `export DATA_SOURCE=/path/to/data.xlsx` |
| SQL Server | 生产(强依赖) | `export DATA_SOURCE_SQL='mssql+pyodbc://...'` |
| PostgreSQL | 生产(可选用 pgvector) | `export DATA_SOURCE_SQL='postgresql+psycop2://...'` |
| SQLite | 测试 | `export DATA_SOURCE_SQL='sqlite:////path/to/test.db'` |

数据源在 server 启动时**加载到内存**,前端只负责对话交互,不接触原始数据,避免数据泄露。

### 3.3 LLM 双重 LLM 协同(可扩展)

当前单 LLM 完成所有任务。架构支持切换 LLM:
- 开发:`minimax`(Mavis 自带 M3,响应快)
- 生产推荐:DeepSeek(便宜、中文好、可私有化部署)
- 配置:环境变量 `LLM_BASE_URL` / `LLM_MODEL`

### 3.4 流式响应(SSE)

`/api/analyze` 用 Server-Sent Events 流式返回,前端实时显示:
- 思考中 → 生成代码 → 执行中 → 结果 → (可选)AI 洞察中 → 完成
- 用户体验:打字机式反馈,不是黑盒等待

### 3.5 单 HTML 文件前端

前端是单文件 `web/static/index.html`(1808 行),内嵌 HTML+CSS+JS:
- **零构建**:不用 webpack/vite,改完即用
- **自建 CSS 设计系统**:中性色 + 单靛蓝强调色,全部图标为内联 SVG(不依赖 emoji 字体),CDN 只加载 ECharts / marked.js
- **可在内网部署**:所有依赖要么 Python 包要么 CDN,启动后浏览器开 `http://server:8765` 即可

---

## 4. 实际部署情况(Demo)

### 4.1 当前 Demo 环境

| 项 | 值 |
|---|---|
| 服务器 | macOS 本地 |
| Python | 3.11(`/opt/homebrew/bin/python3.11`) |
| 端口 | 8765 |
| 数据源 | Excel `/Users/ronnieliu/Downloads/测试数据.xlsx`,sheet=`组件` |
| 数据规模 | 91,392 行 × 60 列 |
| LLM | minimax API(M3 模型) |
| 业务规则 | 3 条默认 + 用户加的若干条 |
| 启动命令 | `cd /Users/ronnieliu/Documents/code/DataAgent && ./web/start.sh` |
| 浏览器访问 | `http://localhost:8765` |

### 4.2 实际数据流转验证

针对真实数据做了端到端测试:

| 测试场景 | 结果 |
|---|---|
| "学术接受度增长率 top10" | ✓ LLM 输出正确代码,自动过滤度量值、按代表聚合、算增长率、取 top10 |
| "川渝大区增长率" | ✓ LLM 自动过滤到 川渝大区(memory 偏好 + 川渝大区 sample question 触发) |
| "同比增长率 top10" | ✓ LLM 用 calculation_rules 里的 `(cur-prev)/prev` 公式,处理除零 |
| 一键出图 | ✓ 自动选柱状图,ECharts 渲染 |
| 一键出报告 | ✓ 生成结构化中文报告,5 章节齐 |
| 业务人员手动改 rules.json | ✓ 重启后立即生效,前端表单可改可加可删 |
| 用户习惯自动学习 | ✓ 单次提问不写,重复/明确才写,置信度 0.5 以下不注入 prompt |
| 冲突防护 | ✓ 用户习惯写 `decimal_places: 4`(0.95 置信),被业务规则 `decimal_places: 2` 自动屏蔽 |

### 4.3 性能指标

| 指标 | 值 |
|---|---|
| 简单问题端到端响应(出数据) | 30-40 秒(LLM 思考 + 代码生成 + 执行) |
| 含 AI 洞察(快路径) | 50-60 秒 |
| 含详细报告(慢路径) | 60-90 秒 |
| 习惯自动学习(后台异步) | < 2 秒,失败不影响主流程 |
| 重启 server | < 5 秒 |

### 4.4 安全 / 合规设计

- **数据不流出**:启动时加载到内存,前端只接收处理后的结果
- **代码沙箱**:LLM 生成代码不能访问文件/网络,只能算数据
- **业务规则审计**:所有规则变更落 `rules.json`,git 可见
- **管理端 token 鉴权**:`ADMIN_TOKEN` 环境变量保护配置端点,默认 `dataagent-admin`,生产必须改
- **使用 token 区分**:生产可加 user login 区分用户,审计"谁问的"

---

## 5. 未来可扩展功能(优先级排序)

### 5.1 P0 - 上生产前必做(预计 1-2 周)

| 功能 | 价值 | 实现成本 |
|---|---|---|
| **多用户 + 权限表** | 不同代表 / 区域经理看到不同数据,审计谁问的 | 接入 SQL Server 用户表,加 `where user_id = X` 过滤 |
| **数据脱敏** | 敏感字段(代表姓名、医院名)在结果里打码 | 加 mask 字典 + 渲染前脱敏 |
| **Docker 沙箱** | 真正的代码隔离,防止 sandbox bypass | 改 `run_sandboxed` 为子进程 + 资源限制 |
| **HTTPS + Nginx** | 传输加密,公网可访问 | 加 nginx 反向代理 + 证书 |
| **Admin Token 强密码** | 防 admin 被白嫖 | 改默认 token,加密码复杂度校验 |

### 5.2 P1 - 3 个月内(预计 2-4 周)

| 功能 | 价值 | 实现成本 |
|---|---|---|
| **PDF 导出报告** | 老板可以直接打印或邮件分享 | 加 weasyprint 或 wkhtmltopdf |
| **历史会话持久化** | 业务人员下次登录能看到上次问的 | 当前 session 在内存,加 PostgreSQL 存储 session 历史 |
| **图表更多类型** | 业务人员用得更顺手 | 加桑基图、热力图、地图(中国地图)等 |
| **多数据源同时问** | 跨表关联查询(代表 + 产品 + 医院) | 加多表 union/join 逻辑 |
| **语音输入** | 销售总监手机直接语音问 | 集成讯飞 / Whisper 语音转文字 |

### 5.3 P2 - 6 个月内(预计 4-8 周)

| 功能 | 价值 | 实现成本 |
|---|---|---|
| **预测模型集成** | 不止"过去发生了什么",还能"下季度会怎样" | 接 Prophet / sklearn 简单回归,LLM 解释模型输出 |
| **自动异常检测** | "G-浙皖大区本周学术接受度异常下降 30%" 自动 push 给相关人 | 加时序异常检测 + 微信/邮件推送 |
| **pgvector 语义检索** | "之前谁问过类似问题"复用经验 | 加 embedding + 向量库(已经有 pgvector 依赖) |
| **Agent 协作(多 AI)** | 一个分析任务多 AI 分工(数据清洗 AI + 洞察 AI + 报告 AI) | 当前架构已经预留,只需拆分 |
| **移动 App** | 销售总监出差路上随时问 | React Native / Flutter 包装前端 |

### 5.4 P3 - 探索性(看 ROI 决定)

| 功能 | 价值 | 风险 |
|---|---|---|
| **接入 BI 工具(Tableau/Power BI)** | 已经有 BI 的企业平滑过渡 | 工作量大,可能不必要 |
| **实时数据(流式)** | 看到当天的实时学术活动 | 数据流接入成本高,业务可能不需要 |
| **跨语言支持(英文)** | 跨国药企 | LLM 切换 Prompt 即可,工作不大 |
| **行业模板库**(心血管、糖尿病、肿瘤) | 不同治疗领域的开箱即用 | 模板维护成本 |

---

## 6. 风险与缓解

| 风险 | 严重度 | 缓解措施 |
|---|---|---|
| LLM 算出错的指标 | 高 | calculation_rules 配置 + 输出代码审计(展示给业务人员看) |
| 敏感数据泄露 | 高 | 数据不流出 + 结果脱敏 + 沙箱隔离 |
| 业务人员乱问导致性能问题 | 中 | 加 rate limit + 异步队列 |
| 业务规则改坏导致 LLM 全错 | 中 | rules.json git 版本管理 + 改前 review |
| LLM API 不稳定 | 中 | 多 LLM 兜底(minimax/DeepSeek/OpenAI 可切换) |
| 用户习惯被错误学习 | 中 | 强信号才写(重复 2 次以上 / 明确表达) + 手动可删 |
| 管理员和用户偏好冲突 | 低 | 三层优先级防护已经处理 |

---

## 7. 投入产出估算

### 7.1 已投入(到 v1.0 Demo)

| 项 | 估时 |
|---|---|
| 需求拆解 + 架构设计 | 1 周 |
| CLI 版(data_agent.py) | 2 天 |
| Web 基础版(server + 前端) | 1 周 |
| 多轮对话 + SSE 流式 | 3 天 |
| 一键出图/出报告 | 3 天 |
| 后台配置 UI(规则结构化) | 2 天 |
| 用户习惯记忆(自动学习 + 冲突防护) | 3 天 |
| 端到端测试 + 调优 | 2 天 |
| **合计** | **约 4 周** |

### 7.2 上生产预计投入(P0)

| 项 | 估时 |
|---|---|
| 多用户 + 权限(对接 SQL Server 用户表) | 1 周 |
| 数据脱敏(字典 + 渲染) | 3 天 |
| Docker 沙箱 | 1 周 |
| HTTPS + Nginx 部署 | 2 天 |
| 性能压测 + 调优 | 1 周 |
| **合计 P0** | **约 4 周** |

### 7.3 ROI 估算(假设)

| 项 | 估算 |
|---|---|
| 数据组同事 1 人,日均处理 20 个业务问题 | 1 FTE |
| 单问题平均处理时间 | 30 分钟 |
| 用 DataAgent 后,业务人员自答率 | 50%(估计保守值) |
| 释放出来的时间 | 0.5 FTE ≈ 10 万元/年 |
| 决策速度提升(问题 2 天 → 30 秒) | 业务上很难量化但显著 |
| 合规审计留痕(《医药代表管理办法》要求) | 合规风险下降,无法直接量化但重要 |

---

## 8. 演示路径(给老板看)

### 第一步:打开主页

`http://localhost:8765` —— 看到简洁的对话界面,顶部有 4 个快捷示例问题

### 第二步:问个真问题

点 "top10 增长率" → 看到 30 秒内出结果 → 一行行解释给老板看

### 第三步:展示多轮对话

在结果基础上问 "只看川渝大区" → 看到 LLM 理解上下文,只查川渝

### 第四步:展示 AI 洞察和报告

点 "AI 洞察" → 看到 LLM 用具体数字说业务洞察
点 "生成图表" → 看到自动选柱状图
点 "生成分析报告" → 看到 5 章节中文报告

### 第五步:展示后台配置

点 "配置" → 让老板看业务规则是怎么配的(表格形式,不是 JSON)
点 "记忆" → 展示自动学到的用户偏好,让老板相信系统会越来越懂业务人员

---

## 9. 结论与建议

### 结论

DataAgent v1.0 Demo **已达到合格可演示状态**,核心功能闭环跑通:
- ✅ 业务人员能用中文问数,30 秒出结果
- ✅ 多轮对话、洞察、图表、报告全套
- ✅ 业务规则可视化配置,无 JSON
- ✅ 用户习惯自动学习 + 手动管理
- ✅ 沙箱安全 + 数据不出内网 + 合规留痕
- ✅ 管理员/用户三层优先级,合规底线不被突破

### 下一步建议

| 优先级 | 建议 |
|---|---|
| **P0** | 先找 1-2 个真实业务人员(销售总监、地区经理)试用 1 周,收集反馈,确认真正"高频问题"是否覆盖 |
| **P0** | 同步启动多用户 + 数据脱敏 + Docker 沙箱的生产化改造 |
| **P1** | 把"数据组同事手工出报告"流程迁移到 DataAgent,数据组解放出来做更高价值分析 |

### 不建议做的事

- ❌ 不要尝试替换 BI 工具(Tableau/Power BI) —— 各有定位,DataAgent 解决"快速取数+洞察",BI 解决"定期看板"
- ❌ 不要尝试做预测模型(2026 年内) —— 先把"过去发生了什么"做透,预测是 P2
- ❌ 不要做成通用聊天机器人 —— 保持"业务+数据"的窄定位,质量比广度重要

---

## 附录:文件清单

```
/Users/ronnieliu/Documents/code/DataAgent/
├── README.md                  # 技术文档(开发看)
├── REPORT.md                  # 本汇报(老板看)
├── requirements.txt           # Python 依赖
├── data_agent.py              # CLI 版(296 行)
├── web/
│   ├── start.sh               # 启动脚本
│   ├── server.py              # FastAPI 后端(1456 行)
│   ├── rules.json             # 业务规则配置(94 行)
│   ├── user_memory.json       # 用户习惯记忆(37 行)
│   └── static/
│       └── index.html         # 前端单文件(1808 行)
└── output/                    # 自动生成的 CSV + 报告样例
```

---

**汇报完毕,期待老板审阅和指正方向。**
